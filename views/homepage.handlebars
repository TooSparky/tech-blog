<div class="container text-center">
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4">
        {{#each blogs as |blog|}}
        <div class="col">{{blog.title}}</div>
        <div class="row">{{blog.content}}</div>
        <div class="row"><a href="/">Comment</a></div>
    </div>
</div>
